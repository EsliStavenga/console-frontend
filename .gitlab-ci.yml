cache:
    untracked: true
    key: ${CI_COMMIT_SHA}
    paths:
        - node_modules/
        - dist/

stages:
    - test
    - deploy


testing:
    stage: test
    image: trion/ng-cli-karma
    allow_failure: false
    environment:
        name: Frontend
    before_script:
        - npm install @angular/cli
        - npm install
    script:
        - ng test --watch=false --code-coverage
    coverage: '/Lines\W +: \d+\% \( \d+\/\d+ \).*/'
    artifacts:
        paths:
            - coverage/
    only:
        - merge_requests
        - master
        - develop
        - staging
#        - feature/CI-CD

deploy_staging:
    stage: deploy
    image: kroniak/ssh-client
    allow_failure: false
    environment:
        name: Frontend
    before_script:
        - echo "$SSH_PUBLIC_KEY" > ~/id_rsa.pub
    script:
        - ssh -i ~/id_rsa.pub www-data@eslistavenga.nl "cd /data/httpd/eslistavenga/pre-prod.eslistavenga.nl/ && echo 'aaaa' > waaaa.html"
    after_script:
        - exit
    only:
        - staging
        - feature/CI-CD

