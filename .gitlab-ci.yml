cache:
    paths:
        - node_modules/

before_script:
    - npm install @angular/cli
    - npm install

testing:
    image: trion/ng-cli-karma
    stage: test
    environment: test
    script:
        - ng test --watch=false --code-coverage
    artifacts:
        paths:
            - coverage/
            - coverage/
    only:
        - merge_requests
        - master
        - develop
        - staging

